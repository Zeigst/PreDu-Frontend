import React, { useContext } from "react"
import { PreduContext } from "../../../../PreduContext"

const Signup = () => {
  const { setOnSignupPage } = useContext(PreduContext)
  
  return (
    <main className="signup">
      <div className="container">
        
      </div>
    </main>
  )
}

export default Signup